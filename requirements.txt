# Web Framework
flask>=3.0.0
flask-restx>=1.3.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# Firma Digital - PDF (PKCS#12 + CMS/PAdES)
# Versión >= 0.25.0 tiene mejor soporte para PDFs con referencias híbridas
pyhanko[openssl]>=0.25.0
# pypdf para normalizar PDFs con referencias híbridas antes de firmar
pypdf>=4.0.0

# Firma Digital - XML (XMLDSig / XAdES)
signxml>=3.2.0
lxml>=5.2.0

# Criptografia (PKCS#12, X.509)
cryptography>=42.0.0

# Utilidades
python-dotenv>=1.0.0

# Servidor de producción
gunicorn>=22.0.0
